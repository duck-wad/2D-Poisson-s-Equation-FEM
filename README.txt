From terminal do "python run.py" to run the python script, which generates the mesh (input parameters need to be modified within the python file), run the C++ executable, and then plot the results on a 3D contour

Rebuild the C++ executable with "sh build.sh" 